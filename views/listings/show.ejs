<%- layout("/layouts/boilerplate.ejs") %>
<script>
  const mapKey = "<%=mapKey%>";
  const listing = <%- JSON.stringify(listing) %>;
</script>

<div class="row mt-3">
  <div class="col-8 offset-3">
    <h3><%=listing.title%></h3>
  </div>
  <div class="card col-6 offset-3 showCard">
    <img
      src="<%=listing.image.url%>"
      class="show-img card-img-top mt-3"
      alt="Listing image"
    />
    <div class="card-body">
      <p class="mt-1"><b>Owned by - </b><i> <%=listing.owner.username%></i></p>
      <p><%=listing.description%></p>
      <p>&#8377;<%=listing.price.toLocaleString("en-IN")%></p>
      <p><%=listing.location%></p>
      <p><%=listing.country%></p>
    </div>
  </div>

  <%if(currentUser._id.equals(listing.owner._id)){%>
  <div class="btns">
    <a
      href="/listings/<%=listing._id%>/new"
      class="btn btn-dark col-1 offset-3 edit-btn mt-2 mb-2"
      >Edit</a
    >
    <form method="POST" action="/listings/<%=listing._id%>?_method=DELETE">
      <button class="btn btn-dark offset-5 mt-2 mb-2">Delete</button>
    </form>
  </div>
  <%}%>
</div>

<div class="row">
  <div class="col-6 offset-3 mt-3 mb-3">
    <%if(currentUser){%>
    <hr />
    <h4>Leave a review</h4>
    <form
      action="/listings/<%=listing._id%>/reviews"
      method="POST"
      novalidate
      class="needs-validation"
    >
      <label>Rating</label>
      <div class="mt-2 mb-1">
        <fieldset class="starability-slot">
          <input
            type="radio"
            id="no-rate"
            class="input-no-rate"
            name="rating"
            value="1"
            checked
            aria-label="No rating."
          />
          <input type="radio" id="first-rate1" name="rating" value="1" />
          <label for="first-rate1" title="Terrible">1 star</label>
          <input type="radio" id="first-rate2" name="rating" value="2" />
          <label for="first-rate2" title="Not good">2 stars</label>
          <input type="radio" id="first-rate3" name="rating" value="3" />
          <label for="first-rate3" title="Average">3 stars</label>
          <input type="radio" id="first-rate4" name="rating" value="4" />
          <label for="first-rate4" title="Very good">4 stars</label>
          <input type="radio" id="first-rate5" name="rating" value="5" />
          <label for="first-rate5" title="Amazing">5 stars</label>
        </fieldset>
      </div>
      <!--  -->
      <div>
        <label for="comment" class="form-label">comment</label>
        <textarea
          name="comment"
          id="comment"
          rows="5"
          cols="30"
          class="form-control"
          required
        ></textarea>
        <div class="invalid-feedback">Comment is required</div>
      </div>
      <button class="btn btn-outline-dark mt-3">Submit</button>
    </form>
    <%}%>
    <hr />

    <%if(listing.reviews.length>0){%>
    <div class="row mt-3">
      <h4>All Reviews</h4>
      <%if(listing.reviews.length > 0){%> <%for(let review of
      listing.reviews){%>
      <div class="card col-5 ms-3 mb-2">
        <div class="card-title"><b>@<%=review.author.username%></b></div>
        <p class="starability-result" data-rating="<%= review.rating %>"></p>
        <p class="card-text"><%= review.comment%></p>
        <form
          action="/listings/<%=listing._id%>/reviews/<%=review.id%>?_method=DELETE"
          method="POST"
        >
          <button class="btn btn-dark mt-2 mb-2">Delete</button>
        </form>
      </div>
      <%}%> <%}else{%>
      <p>No Reviews have been made</p>
      <%}%>
    </div>
    <%}%>
  </div>
  <div class="col-6 offset-3 mt-3 mb-3">
    <h3>Where you'll be</h3>
    <div id="map"></div>
  </div>
</div>

<script src="/js/maptiler.js"></script>
